- Fixed backup source path entry so pressing Enter keeps the manually typed share-root path and autocomplete suggestions preserve the visible `/mnt/user/...` prefix instead of switching to a resolved pool path.
