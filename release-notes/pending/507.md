### Improvements

- Administrators can delete individual archives from the Repositories archive list after confirming the selected backup. Existing jobs and run history are retained; use Compact to reclaim disk space afterwards. (#507)
