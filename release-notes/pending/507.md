### Improvements

- Administrators can delete individual archives from the Repositories archive list. The confirmation shows the repository and archive details and requires entering DELETE. Existing jobs and run history are retained; use Compact to reclaim disk space afterwards. (#507)
