### Improvements

- Restore several files and folders from one archive in a single operation. Browse an expandable folder tree, filter the current folder and keep your selection while navigating. Review every source, planned action and destination in a separate restore-plan table before starting. (#521)
    - The first archive opening skips single-folder chains automatically, so you reach the contents without entering a full path.
    - Folder structure below the common parent is retained; overlapping selections are restored only once.
    - The technical precheck lists every selected entry and its destination, and clearly distinguishes metadata validation from the actual dry run or restore.
    - The existing dry-run option now performs a Borg simulation without restoring or replacing files, with a clear result in the live view and history.
