### Bug Fixes

- USB backups now accept an existing subfolder on a mounted drive, matching the profile status check. Existing storage and repository paths remain unchanged. Missing drives, read-only targets and access errors still prevent backup startup. (#516)
