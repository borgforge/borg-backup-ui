### Improvements

- Browse & Restore can show all archives in the selected repository or use a custom archive name pattern, including Nextcloud AIO archives. The job's current and historical archive prefixes remain the default. Backup names and retention rules are unchanged (#537).
