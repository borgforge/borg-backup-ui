### Bug Fixes

- Reduce unnecessary writes to the Unraid boot USB stick during idle operation and normal browsing. Unchanged notification data is no longer repeatedly saved, and Python's temporary bytecode cache stays in RAM. (#497)
