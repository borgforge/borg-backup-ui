- Fixed a Docker backup concurrency race: overlapping Docker-enabled jobs now acquire the Docker control lock whenever Docker runtime control is configured, and Docker/VM restart recovery state is written safely across processes.
