- Changed the Unraid dashboard widget to use event-based cache updates instead of periodic status scans, so dashboard refreshes continue to read only the flash cache and do not wake storage disks.
- Added cached job metadata so the dashboard widget can still show newly overdue backups based on the cached data timestamp.
- Builds the first usable widget cache once from existing status and restore-test files when no compatible flash cache exists yet.
- Keeps the widget cache timestamp stable during normal Borg Backup UI status polling; it now changes only when the widget cache is actually rebuilt.
