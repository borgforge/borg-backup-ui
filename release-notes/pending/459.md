### Improvements

- Choose tiered retention with hourly rules and a recent time window, keep the last X archives, or keep all. Clearly visible info buttons open an overview of active rules with examples of what is kept or deleted. Input fields are aligned across columns. Empty and time-window-only policies are blocked in the wizard and both prune paths. Maintenance logs show the archive filter and applied rules together without duplicate policy lines. (#459)
