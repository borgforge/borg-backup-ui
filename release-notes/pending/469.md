### Improvements

- Exclude entire folders using case-sensitive marker filenames configured per job. These rules supplement existing path exclusions. A dedicated Exclusions step shows path, marker and file rules together without expanding the wizard. (#469)
