- Fixed the Unraid dashboard widget cache so a stale running-only snapshot no longer blocks rebuilding from completed backup status files, and backup finalization now refreshes the widget cache after status persistence.
