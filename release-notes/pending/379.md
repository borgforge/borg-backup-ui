- Added Borg key recovery for encrypted repositories: required Borg key exports are included in the encrypted jobs/passphrases transfer file, the import flow now uses a multi-step wizard with job selection, action choice, and final confirmation instead of exposing the technical preview table, repositories show a warning when a key backup is missing, and manual key import/export checks the repository ID before accepting a key.
