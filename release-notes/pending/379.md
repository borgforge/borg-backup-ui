- Added Borg key recovery for encrypted repositories: jobs/passphrases, repository keys, and profiles/secrets are handled as separate encrypted transfer flows, repository-key backups can contain all exported repository keys, restoring a key is done for one selected repository with repository-ID validation, and the job import wizard now focuses on complete or selective job/passphrase recovery with clearer review details.
