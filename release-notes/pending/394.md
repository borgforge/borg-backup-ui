- Fresh installations no longer start runtime write services with fallback `/mnt/user` status paths before the setup wizard has configured the data directory, and configured `/mnt` data directories are only created once their storage mount is available.
