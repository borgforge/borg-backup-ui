- Added a Borg serve only mode for SSH storage profiles so restricted SSH targets without a normal shell can be used for Borg repositories.
- Skipped shell-based path and write checks for Borg serve only profiles; repository access is validated by Borg during repository import or creation.
