- Introduce a guided migration to permanent job identities: prepare and verify a protected backup, save and acknowledge a separate copy, then explicitly start conversion (#447, #479).
- Keep normal plugin operations paused until migration and final integrity checks succeed; retain the original plan, backup and progress across interruptions and require validated, explicit continuation (#479).
- Keep the migration assistant accessible without an overlapping setup dialog; new installations continue to use normal first-run setup (#479).
- Migrate valid existing restore-test reports without false identity errors. Show preparation progress and lasting, readable failure messages with clearer backup-directory guidance (#447, #479).
- Provide German and English migration guidance. Back up the Unraid flash configuration separately; the first test-channel candidate requires volunteer testing before stable release approval (#479).
- Preserve applicable pre-migration restore proof and recorded repository-check values. Explain missing target evidence separately from expired tests, show migrated-run information only in expanded history details, and remove empty identity cells (#447, #479).
- Restore complete sanitized job and configuration diagnostics in support bundles, including sources, exclusions, prefix history and policies; keep structured run and restore reports readable as complete JSON (#447, #479).
- Preserve automatic job icons and colors during migration and restore them for unchanged migrated jobs using verified original migration data. Keep explicit icon/color choices, use the correct icon colors throughout the UI, and sort job selections by location and name instead of job ID (#447, #479).
