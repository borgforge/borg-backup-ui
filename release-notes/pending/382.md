- Uninstall now removes the Borg Backup UI plugin payload from `/boot/config/plugins/borg-backup-ui` as well as runtime hooks installed by the plugin, while leaving Borg repositories and user data outside the plugin directory untouched.
- Repository Borg-key warnings now link to Settings > Import / Export, and the Borg-key export shows a visible running state while the encrypted key backup is being created.
