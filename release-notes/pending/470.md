### Improvements

- Upload one exclusion-pattern file per job, with a managed copy that can be downloaded, replaced or removed and travels with job exports. Missing or corrupt copies block backup instead of silently omitting the exclusions. (#470)

### Migration and compatibility

- Job exports with extended retention or exclusions use a new bundle format that older versions reject. Existing UUID job bundles remain importable. (#470)
