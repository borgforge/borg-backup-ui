### Improvements

- **Optional Prometheus and Grafana integration:** Enable read-only metrics in Settings > Integrations and import the included Grafana dashboard for backup results, sizes, repository statistics and restore-test evidence. (#534)
    - Disabled by default, with a separate revocable access token and German/English setup guidance.
    - Cached collection reads existing plugin data without running Borg or refreshing repositories. Missing evidence and collector failures remain visible.
    - Redesigned Grafana dashboard with the plugin icon, active jobs, equal-sized job cards, storage efficiency and consolidated backup, recovery and repository details.
