- Keep the live log responsive for large backups when optional file activity is enabled, while retaining every emitted log entry (#463).
- Browse earlier log sections and download the full log directly from the file-activity view. Jobs without file activity keep their existing live-log behavior (#463).
- Keep the live view filled during small updates and make its controls readable in both light and dark themes (#463).
- Show completed file-activity runs with Borg exit code 1 as warnings in the log panel (#463).
- Keep active file-activity logs in RAM and save the complete log after the backup ends, avoiding changing-log warnings when the log directory is backed up. The active log is not part of that backup; a server restart before it is saved loses the volatile log (#463).
