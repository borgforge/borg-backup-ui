- Keep the live log responsive for large backups when optional file activity is enabled, while retaining every emitted log entry (#463).
- Browse earlier log sections, search the complete run log, and download the full log directly from the file-activity view. Jobs without file activity keep their existing live-log behavior (#463).
