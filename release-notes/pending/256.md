- Added a dedicated Notifications settings area with Apprise profile management, compact saved-profile summaries, searchable provider selection, dynamic provider URL templates from Apprise metadata, URL validation and test delivery while keeping native ntfy available separately.
- Apprise profile editing now generates provider-specific URL fields from the selected URL format, saves non-secret fields for later edits and keeps password/token values write-only so they can be replaced without being rendered back into the browser.
