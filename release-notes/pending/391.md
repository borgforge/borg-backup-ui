- Wait for the mount backing the configured data directory before starting runtime services, including user shares, pools, array disks, and Unassigned Devices paths.
- Prevent status, restore-test, and snapshot paths from being created below an unavailable `/mnt` mount.
- Improve early-start storage diagnostics and include the Borg Backup UI version in the startup log.
