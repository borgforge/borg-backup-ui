Recover incomplete manual plugin deletions during reinstall: if Unraid still reports the package as installed but plugin payload or Apprise vendor metadata is missing, the installer now re-extracts the verified package before continuing.
