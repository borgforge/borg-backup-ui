- Scoped Borg prune retention to the current job's archive prefix so shared repositories do not prune archives from other jobs, including an explicit retention-source selection for manual prune on repositories used by multiple jobs.
