(() => {
  const sections=[['general','⚙','Allgemein','System, Pfade und E-Mail','System'],['users','♙','Benutzer','Zugriff und Sessions','System'],['backup','▣','Backup','Docker, VMs und Reports','Betrieb'],['restore','✓','Restore','Tests, Limits und Performance','Betrieb'],['usb','▯','USB-Profile','Lokale Datenträger','Speicherziele'],['smb','⌁','SMB-Profile','Netzwerkfreigaben','Speicherziele'],['ssh','↗','SSH-Profile','Remote-Ziele und Schlüssel','Speicherziele'],['transfer','⇄','Import / Export','Transfer und Config-Backups','Wartung'],['advanced','⌘','Erweitert','Passphrasen und technische Daten','Wartung']];const requestedSection=new URLSearchParams(location.search).get('section');let active=sections.some(section=>section[0]===requestedSection)?requestedSection:'general';
  const badge=(text,cls='')=>`<span class="badge ${cls}">${text}</span>`;const field=(label,value,type='text',hint='')=>`<label class="field"><span>${label}</span><input type="${type}" value="${value}">${hint?`<small>${hint}</small>`:''}</label>`;const select=(label,options,selected=0)=>`<label class="field"><span>${label}</span><select>${options.map((o,i)=>`<option ${i===selected?'selected':''}>${o}</option>`).join('')}</select></label>`;const panel=(title,subtitle,body,action='')=>`<section class="settings-panel"><header><div><strong>${title}</strong><small>${subtitle}</small></div>${action}</header><div class="panel-body">${body}</div></section>`;
  const health=()=>panel('Systemzustand & Migration','Integrität der Plugin-Konfiguration',`<div class="health-overview"><span class="health-mark">✓</span><div><strong>Systemzustand OK</strong><small>Alle System- und Jobprüfungen sind erfolgreich.</small></div>${badge('OK','ok')}</div><div class="health-grid"><span>Data Root <b>OK</b><small>/mnt/user/borg-backup-ui</small></span><span>Jobs-Pfad <b>OK</b><small>11 Jobs geprüft</small></span><span>Secrets-Pfad <b>OK</b><small>7 Dateien geprüft</small></span><span>Letzte Migration <b>Erfolgreich</b><small>Keine Änderungen nötig</small></span></div><details><summary>Technische Details und Migrationsstatus</summary><pre>Migration-State: /boot/config/plugins/borg-backup-ui/migration-state.json\nMigration-Log: /var/log/borg-backup-ui/migration.log\nRegistry: 12 Einträge · 0 offen · 0 fehlerhaft</pre></details>`);
  const general=()=>`${workspace('Allgemein','System, Datenpfade, Benachrichtigungen und Versionsinformationen')}${health()}${panel('Allgemein','Zentrale Daten- und Laufzeiteinstellungen',`<div class="form-grid">${field('Hauptverzeichnis (Pflicht)','/mnt/user/borg-backup-ui','text','GLOBAL_DATA_DIR')}${field('Log-Verzeichnis (abgeleitet)','/mnt/user/borg-backup-ui/logs')}${field('Status-Verzeichnis (abgeleitet)','/mnt/user/borg-backup-ui/status')}${field('Restore-Test-Status (abgeleitet)','/mnt/user/borg-backup-ui/restore-tests')}${select('UI-Theme',['Dunkel','Hell','System'])}${field('Log-Aufbewahrung (Tage)','30','number')}${field('Borg Cache-Verzeichnis','/mnt/cache/borg')}${field('Check-Intervall (Tage)','30','number')}</div><label class="check"><input type="checkbox" checked>Backup abbrechen, wenn Parity-Check läuft</label>`)}${panel('E-Mail (SMTP)','Benachrichtigungen und Testversand',`<div class="form-grid">${field('Empfänger','admin@example.net','email')}${field('Absender','borg@tower.local','email')}${field('SMTP-Host','smtp.example.net')}${field('SMTP-Port','587','number')}${field('SMTP-Benutzer','borg-ui')}${field('SMTP-Passwort','••••••••','password')}</div><label class="check"><input type="checkbox" checked>TLS/STARTTLS verwenden</label><button class="btn">Test-E-Mail senden</button>`)}${panel('Über Borg Backup UI','Installierte Komponenten',`<dl class="about"><div><dt>Version</dt><dd>2026.06</dd></div><div><dt>Borg Version</dt><dd>1.4.1</dd></div><div><dt>Autor</dt><dd>Thorsten Steinberg</dd></div><div><dt>Lizenz</dt><dd>MIT</dd></div><div><dt>Drittanbieter</dt><dd>BorgBackup · BSD-3-Clause</dd></div></dl>`)}`;
  const users=()=>`${workspace('Benutzer','Konten, Rollen und aktive Sitzungen')}${panel('Sitzungen','Aktuell angemeldeter Benutzer',`<div class="user-session"><span>Angemeldet <b>admin (admin)</b></span><span>Eigene Sessions <b>1</b></span><span>Gesamt <b>2</b></span></div><div class="button-row"><button class="btn">Eigenes Passwort ändern</button><button class="btn">Meine Sessions abmelden</button><button class="btn">Alle Sessions abmelden</button></div>${field('Session-Timeout (Minuten)','30','number')}`)}${panel('Benutzerverwaltung','Konten anlegen und bearbeiten',`<div class="create-user">${field('Benutzername','newuser')}${select('Rolle',['viewer','operator','admin'])}${field('Passwort','','password','mindestens 12 Zeichen')}<button class="btn primary">Anlegen</button></div><div class="table-wrap"><table><thead><tr><th>Benutzer</th><th>Rolle</th><th>Aktiv</th><th>Letzter Login</th><th>Aktionen</th></tr></thead><tbody><tr><td>admin</td><td><select><option>admin</option></select></td><td>✓</td><td>19.06.2026, 09:08</td><td><button class="btn sm">Speichern</button> <button class="btn sm">Passwort</button></td></tr><tr><td>operator</td><td><select><option>operator</option></select></td><td>✓</td><td>18.06.2026, 17:42</td><td><button class="btn sm">Speichern</button> <button class="btn sm danger">Deaktivieren</button></td></tr></tbody></table></div>`)}`;
  const backup=()=>`${workspace('Backup','Verhalten während Sicherungen und Statusberichte')}${panel('Docker & VMs','Stop-, Start- und Wartezeiten',`<div class="form-grid">${field('Docker Stop Timeout (s)','120','number')}${field('Docker Stop Wartezeit (s)','10','number')}${field('Docker Start Wartezeit (s)','15','number')}${field('VM Shutdown Timeout (s)','300','number')}${field('VM Warnung (Minuten)','5','number')}${field('VM Start Wartezeit (s)','30','number')}</div>`)}${panel('Wöchentlicher Status-Report','Automatischer Bericht per E-Mail',`<label class="check"><input type="checkbox" checked>Report aktivieren</label><div class="form-grid">${select('Wochentag',['Montag','Dienstag','Mittwoch','Donnerstag','Freitag'])}${field('Uhrzeit','09:00','time')}${field('Empfänger','admin@example.net','email')}</div><button class="btn">Jetzt senden</button>`)}`;
  const restore=()=>`${workspace('Restore','Standardwerte für Restore-Tests')}${panel('Restore Tests','Basis, Dry-Run und Performance-Limits',`<h4>Basis</h4><div class="form-grid">${select('Standard Test-Level',['L1','L2','L3'],1)}${field('Intervall (Tage)','30','number')}${select('Standard Location',['Local','USB','SMB','Storagebox','Alle'])}</div><h4>Strategie Dry-Run</h4><div class="form-grid">${field('Chunk-Modus erzwingen für Typen','vms,photos')}${field('Ab Größe direkt Chunk-Modus (GB)','500','number')}</div><h4>Limits & Performance</h4><div class="form-grid">${field('Mindest-Coverage (%)','5','number')}${field('Max. Einträge je Test','1000','number')}${field('Level-3 Sample-Dateien','5','number')}${field('Borg Standard Timeout (s)','240','number')}${field('Dry-Run Timeout (s)','0','number')}${field('Chunk-Größe (Dateien)','100','number')}${field('Max. Dry-Run Dateien','1000','number')}</div><label class="check"><input type="checkbox">Level-3 Legacy-Sampling verwenden</label>`)}`;
  const profileView=(kind)=>{const isUsb=kind==='usb',isSmb=kind==='smb';const title=isUsb?'USB-Profile':isSmb?'SMB-Profile':'SSH-Profile';const description=isUsb?'Lokale Datenträger als Backup-Ziele':isSmb?'Netzwerkfreigaben mit separaten Credentials':'Remote-Ziele, Verbindung und SSH-Schlüssel';const name=isUsb?'USB-Drive-A':isSmb?'NAS-A':'Storagebox';const endpoint=isUsb?'/mnt/disks/BACKUP':isSmb?'//192.168.178.50/backup':'u525674.your-storagebox.de:23';const jobsCount=isUsb?'4':isSmb?'1':'3';const fields=isUsb?`${field('Profilname','USB-Drive-A')}${field('Mount-Pfad','/mnt/disks/BACKUP')}`:isSmb?`${field('Profilname','NAS-A')}${field('Host / IP','192.168.178.50')}${field('Share','backup')}${field('Mount-Pfad','/mnt/user/borg-backup-ui/remotes/nas-a')}${field('Benutzer','borg')}${field('Passwort','••••••••','password')}${field('SMB-Version','3.0')}${field('Security','ntlmssp')}`:`${field('Profilname','Storagebox')}${field('Host','u525674.your-storagebox.de')}${field('Port','23','number')}${field('Benutzer','u525674')}${field('Basis-Pfad','./backup')}${field('SSH-Key','/root/.ssh/id_rsa')}${select('Zieltyp',['Storagebox','Synology','Generic SSH'])}`;const checks=isUsb?[['Mount vorhanden','OK'],['Datenträger beschreibbar','OK'],['Freier Speicher','2,81 TB']]:isSmb?[['SMB-Port 445','OK'],['Authentifizierung','OK'],['Share gefunden','OK'],['Test-Mount / Schreiben','OK']]:[['Host erreichbar','OK'],['SSH-Authentifizierung','OK'],['Basis-Pfad verfügbar','OK'],['Schreibzugriff','OK']];const setupActions=isUsb?'<button class="btn">Status prüfen</button>':isSmb?'<button class="btn">Status prüfen</button><button class="btn">Verbindung testen</button>':'<button class="btn">Status prüfen</button><button class="btn">Key erzeugen</button><button class="btn">Public Key anzeigen</button><button class="btn">Key deployen</button><button class="btn">Verbindung testen</button>';return `${workspace(title,description)}<div class="profile-manager"><aside class="profile-list"><header><div><strong>Gespeicherte Profile</strong><small>1 Profil</small></div></header><button class="profile-list-item active"><span class="profile-symbol">${isUsb?'▯':isSmb?'⌁':'↗'}</span><span><strong>${name}</strong><small class="mono">${endpoint}</small><em>${jobsCount} zugeordnete Jobs</em></span><b class="profile-state">✓</b></button><footer><button class="btn primary">＋ ${isUsb?'USB':isSmb?'SMB':'SSH'}-Profil hinzufügen</button></footer></aside><section class="profile-editor readonly"><header><div><span class="label">Ausgewähltes Profil</span><h3>${name}</h3><small>${endpoint}</small></div><div class="profile-header-actions">${badge(isUsb?'Lokal':isSmb?'SMB':'SSH','info')} ${badge('Bereit','ok')} <button class="btn sm" data-profile-edit>Bearbeiten</button></div></header><div class="profile-editor-body"><div class="profile-meta"><span>Profil-Schlüssel <b class="mono">${isUsb?'usb-drive-a':isSmb?'nas-a':'storagebox'}</b></span><span>Zugeordnete Jobs <b>${jobsCount}</b></span></div><div class="form-grid">${fields}</div><div class="profile-editor-actions"><button class="btn danger">Profil entfernen</button><div class="profile-edit-actions" hidden><button class="btn" data-profile-cancel>Abbrechen</button><button class="btn primary" data-profile-save>Profil speichern</button></div></div></div></section></div>${panel(`${isUsb?'USB':isSmb?'SMB':'SSH'} Setup & Check`,'Prüft das aktuell ausgewählte Profil',`<div class="connection-overview"><span class="connection-mark">✓</span><div><strong>Profil einsatzbereit</strong><small>Letzte Prüfung: 19.06.2026, 09:15</small></div>${badge('Alle Prüfungen OK','ok')}</div><div class="check-list">${checks.map(check=>`<span><b>${check[0]}</b><em>${check[1]}</em></span>`).join('')}</div><div class="button-row">${setupActions}</div>`)}`};
  const transfer=()=>`${workspace('Import / Export','Verschlüsselter Transfer und Konfigurationssicherung')}${panel('Import / Export','Jobs, Passphrasen, Profile und Secrets',`<div class="transfer-grid"><button class="transfer-action"><b>Supportpaket</b><small>Diagnosedaten erstellen</small></button><button class="transfer-action"><b>Jobs + Passphrasen</b><small>Verschlüsselt exportieren</small></button><button class="transfer-action"><b>Jobs-Vorschau</b><small>Importdatei prüfen</small></button><button class="transfer-action"><b>Profile + Secrets</b><small>Verschlüsselt exportieren</small></button><button class="transfer-action"><b>Profil-Vorschau</b><small>Importdatei prüfen</small></button></div>`)}${panel('Config-Backups & Rollback','Beim Speichern wird automatisch ein Backup erstellt',`<div class="table-wrap"><table><thead><tr><th>Backup</th><th>Datum</th><th>Größe</th><th>Aktionen</th></tr></thead><tbody><tr><td class="mono">backup.conf.20260619-091500</td><td>19.06.2026, 09:15</td><td>18,4 KB</td><td><button class="btn sm">Diff</button> <button class="btn sm">Wiederherstellen</button></td></tr><tr><td class="mono">backup.conf.20260618-174200</td><td>18.06.2026, 17:42</td><td>18,2 KB</td><td><button class="btn sm">Diff</button> <button class="btn sm danger">Löschen</button></td></tr></tbody></table></div>`,`<button class="btn sm">Alte Backups aufräumen</button>`)}`;
  const advanced=()=>`${workspace('Erweitert','Passphrasen und technische Repository-Daten')}${panel('Per-Repo Passphrasen','Automatisch beim Anlegen verschlüsselter Jobs erstellt',`<div class="table-wrap"><table><thead><tr><th>Typ</th><th>Pfad auf Flash</th><th>Geändert</th></tr></thead><tbody><tr><td>appdata</td><td class="mono">/boot/config/plugins/borg-backup-ui/secrets/appdata.passphrase</td><td>18.06.2026, 14:22</td></tr><tr><td>photos</td><td class="mono">/boot/config/plugins/borg-backup-ui/secrets/photos.passphrase</td><td>12.06.2026, 09:31</td></tr></tbody></table></div><div class="security-note">Passphrasen werden niemals im Klartext angezeigt oder in Logs ausgegeben.</div>`)}`;
  const workspace=(title,description)=>`<header class="workspace-title"><div><span class="label">Einstellungen</span><h2>${title}</h2><small>${description}</small></div>${badge('Gespeichert','ok')}</header>`;
  const views={general,users,backup,restore,usb:()=>profileView('usb'),smb:()=>profileView('smb'),ssh:()=>profileView('ssh'),transfer,advanced};
  const navHtml=()=>{let group='';return sections.map(section=>{const heading=section[4]!==group?`<div class="settings-group-label">${section[4]}</div>`:'';group=section[4];return `${heading}<button class="settings-item ${active===section[0]?'active':''}" data-section="${section[0]}"><span class="settings-icon">${section[1]}</span><span><strong>${section[2]}</strong><small>${section[3]}</small></span><b>›</b></button>`}).join('')};
  const render=()=>{const profileSection=['usb','smb','ssh'].includes(active);const globalSave=document.getElementById('save-btn');globalSave.hidden=profileSection;document.getElementById('settings-nav').innerHTML=navHtml();document.getElementById('settings-content').innerHTML=views[active]();document.querySelectorAll('[data-section]').forEach(button=>button.addEventListener('click',()=>{active=button.dataset.section;render()}));document.querySelectorAll('input,select').forEach(input=>{if(input.closest('.profile-editor')){input.disabled=true;return}input.addEventListener('change',()=>{globalSave.classList.add('dirty');globalSave.textContent='✓ Änderungen speichern'})});const editor=document.querySelector('.profile-editor');if(editor){const edit=editor.querySelector('[data-profile-edit]'),actions=editor.querySelector('.profile-edit-actions'),controls=editor.querySelectorAll('input,select');const setEditing=enabled=>{editor.classList.toggle('readonly',!enabled);controls.forEach(control=>control.disabled=!enabled);edit.hidden=enabled;actions.hidden=!enabled};edit.addEventListener('click',()=>setEditing(true));editor.querySelector('[data-profile-cancel]').addEventListener('click',()=>setEditing(false));editor.querySelector('[data-profile-save]').addEventListener('click',()=>{setEditing(false);edit.textContent='Bearbeiten'})}};render();
  document.getElementById('save-btn').addEventListener('click',event=>{event.currentTarget.classList.remove('dirty');event.currentTarget.textContent='✓ Gespeichert'});document.documentElement.dataset.theme=localStorage.getItem('settings-study-theme')||'dark';const theme=document.getElementById('theme-toggle');const sync=()=>theme.textContent=document.documentElement.dataset.theme==='dark'?'☀':'☾';sync();theme.addEventListener('click',()=>{const next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('settings-study-theme',next);sync()});
})();
